package Day7_SwitchStatements;

public class Topic2_SwitchStatements {
    public static void main(String[] args) {
        //we use switch statements in a situation where we have a simple condition check but alot of them.
        /*
        variable name
        switch(name){

        case value:
            code block;
            break; //not mandatory

        case value1:
            code block;
            break;
        default: code block
        }
         */
    }
}
